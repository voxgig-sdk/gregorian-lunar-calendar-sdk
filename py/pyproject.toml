[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "voxgig-sdk-gregorian-lunar-calendar"
version = "0.0.1"
description = "Unofficial generated Python SDK for the Gregorian Lunar Calendar public API. Not affiliated with or endorsed by the upstream API provider."
readme = "README.md"
license = "MIT"
requires-python = ">=3.8"
keywords = ["voxgig", "sdk", "generated-sdk", "openapi", "api-client", "gregorian-lunar-calendar"]
dependencies = [
    "requests>=2.33",
]

[project.urls]
Homepage = "https://github.com/voxgig-sdk/gregorian-lunar-calendar-sdk"
Repository = "https://github.com/voxgig-sdk/gregorian-lunar-calendar-sdk"
Issues = "https://github.com/voxgig-sdk/gregorian-lunar-calendar-sdk/issues"

# ONE package. core/entity/feature/utility were previously top-level packages;
# those names are real PyPI distributions and common scratch filenames, and
# Python searches the working directory first, so any of them beside the
# caller silently shadowed the SDK's own. They now live inside
# gregorianlunarcalendar_sdk/ where nothing can reach them.
[tool.setuptools.packages.find]
include = ["gregorianlunarcalendar_sdk*"]

# Ship the PEP 561 py.typed marker(s) so the inline type hints reach consumers.
[tool.setuptools.package-data]
"*" = ["py.typed"]
